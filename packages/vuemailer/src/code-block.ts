// `vuemailer/code-block` — re-export of @vuemailer/core/code-block, kept a separate entry so the optional
// `prismjs` peer is only loaded when consumers import this path.
export * from '@vuemailer/core/code-block'
